{
  "name": "jwtforge",
  "version": "0.0.1",
  "description": "Unpack JWT tokens and manipulate the payloads",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andre McGruder",
  "license": "ISC",
  "dependencies": {
    "base64url": "^3.0.1",
    "nodemon": "^2.0.4"
  }
}
